jdk:
  - openjdk17

env:
  GRADLE_OPTS: '-Xmx2048m'

install:
  - ./gradlew clean publishToMavenLocal -x jsJar -x wasmJsJar -x wasmWasiJar -x jsTest -x wasmJsTest -x wasmWasiTest -x kotlinNpmInstall --no-daemon -Dmaven.repo.local=build/m2repo
